components:
  schemas:
    workspace:
      type: object
      properties:
        # STIX common properties
        workflow:
          type: object
          properties:
            state:
              type: string
